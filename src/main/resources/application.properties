# ? Datenbank-Art definieren (H2)
quarkus.datasource.db-kind=h2

# ? JDBC URL für eine In-Memory-Datenbank (H2)
# DB_CLOSE_DELAY=-1 stellt sicher, dass die DB nicht geschlossen wird, solange die JVM läuft.
quarkus.datasource.jdbc.url=jdbc:h2:mem:default;DB_CLOSE_DELAY=-1

# ? Standard-Zugangsdaten (optional für H2 In-Memory)
quarkus.datasource.username=sa
quarkus.datasource.password=sa

# ? Schema-Generierung: Erstellt die Tabellen basierend auf Ihren Entities beim Start neu
quarkus.hibernate-orm.database.generation=drop-and-create

# ? Optional: Zeigt die generierten SQL-Anweisungen in der Konsole an
quarkus.hibernate-orm.log.sql=true

# ?? Optional: Aktiviert die H2-Webkonsole (praktisch zum Prüfen der Daten)
quarkus.h2-console.enabled=true
quarkus.h2-console.path=/h2-console
quarkus.http.port=8081


